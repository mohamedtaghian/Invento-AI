refactor(user-site): upgrade product feature architecture and styling

- Created public barrel file index.ts for the product feature
- Migrated all internal relative imports to use absolute path aliases
- Removed all wishlist state and UI components as it's no longer needed
- Enforced SpartanUI typography directives across all templates
- Replaced hardcoded Tailwind colors with semantic theme variables
- Eliminated forbidden 'any' types in filters and components
